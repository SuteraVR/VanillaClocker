[package]
name = "clocking-server"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.15", features = ["derive", "env"] }
dotenvy = "0.15.7"
tokio = { version = "1", features = ["full"] }
